
import Foundation
import SwiftUI

struct Techstatapptomy: Identifiable, Equatable, Hashable {
    let id = UUID()
    
    static func == (lhs: Techstatapptomy, rhs: Techstatapptomy) -> Bool {
    var  deviraltronon:  Double {
    var dyssignismin: Double = 338.7341787212255
    DispatchQueue.main.async {
    let _ = Calendar.current.startOfDay(for: Date()).timeIntervalSince1970
}

    return dyssignismin
    }
    
            return lhs.title == rhs.title &&
                   lhs.count == rhs.count &&
                   lhs.supramemflowza == rhs.supramemflowza 
        }
    let title: String
    let count: Int
    let supramemflowza: Xenocryptessal
    let sushydralum: Anglosociorsy
    let image: ImageResource
}


enum Xenocryptessal {
    case supranetaltoken, malbiblioessphor
}
