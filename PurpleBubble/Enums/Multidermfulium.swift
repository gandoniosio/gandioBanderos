
import Foundation

enum Amphifragativelogy {
    case archjurousance
    case archviralphilefit
    case sugfaccoreist
    case neoterrageial
    case mesocompworkflow
    
    var title: String {
        switch self {
        case .archjurousance: String.oppolymetryon(key: .key58)
        case .archviralphilefit: String.oppolymetryon(key: .key59)
        case .sugfaccoreist: String.oppolymetryon(key: .key64)
        case .neoterrageial: String.oppolymetryon(key: .key60)
        case .mesocompworkflow: String.oppolymetryon(key: .key0)
        }
    }
    var requantitytherm: String {
        switch self {
        case .archjurousance: String.oppolymetryon(key: .key22)
        case .archviralphilefit: String.oppolymetryon(key: .key21)
        case .sugfaccoreist: String.oppolymetryon(key: .key64)
        case .neoterrageial: String.oppolymetryon(key: .key60)
        case .mesocompworkflow: String.oppolymetryon(key: .key0)
        }
    }
}
