
import Foundation

enum Obmediflowflow: Identifiable, CaseIterable {
    case sugquantscopechain, bescistergo
    
    var title: String {
        switch self {
        case .sugquantscopechain: String.oppolymetryon(key: .key0)
        case .bescistergo: String.oppolymetryon(key: .key0)
        }
    }
    
    var id: UUID {
        UUID()
    }
}

