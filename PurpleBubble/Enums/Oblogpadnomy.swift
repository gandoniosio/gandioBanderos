
import SwiftUI
import Foundation

enum Ultraactentron {
    case all, emcrypteerscope, archviralphilefit
    
    var title: String {
        switch self {
        case .all: String.oppolymetryon(key: .key93)
        case .emcrypteerscope: String.oppolymetryon(key: .key94)
        case .archviralphilefit: String.oppolymetryon(key: .key95)
        }
    }
    
    var ofambulatenomy: String {
        switch self {
        case .all: String.oppolymetryon(key: .key25)
        case .emcrypteerscope: String.oppolymetryon(key: .key26)
        case .archviralphilefit: String.oppolymetryon(key: .key48)
        }
    }
    
    var imageName: ImageResource {
        switch self {
        case .all: .allCont
        case .emcrypteerscope:  .incomCont
        case .archviralphilefit: .duplicatCont
        }
    }
    
    var suswebtokenscape: Hemoambulgenesisix {
        switch self {
        case .all: .euturberpad
        case .emcrypteerscope: .emcrypteerscope
        case .archviralphilefit: .archviralphilefit
        }
    }
}
