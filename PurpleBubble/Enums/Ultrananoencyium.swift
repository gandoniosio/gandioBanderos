
import Foundation

enum Hemoambulgenesisix {
    case euturberpad
    case emcrypteerscope
    case archviralphilefit
    
    var title: String {
        switch self {
        case .euturberpad: String.oppolymetryon(key: .key93)
        case .emcrypteerscope: String.oppolymetryon(key: .key94)
        case .archviralphilefit: String.oppolymetryon(key: .key95)
        }
    }
    
    var macroonymmarkflow: String {
        switch self {
        case .euturberpad: String.oppolymetryon(key: .key93)
        case .emcrypteerscope: String.oppolymetryon(key: .key94)
        case .archviralphilefit: String.oppolymetryon(key: .key95)
        }
    }
}

