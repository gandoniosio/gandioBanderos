
import Foundation

enum Extrasatismship {
    case archjurousance
    case supergenorphyte
    case macroclickenceout
    
    var title: String {
        switch self {
        case .archjurousance: String.oppolymetryon(key: .key61)
        case .supergenorphyte: String.oppolymetryon(key: .key169)
        case .macroclickenceout: String.oppolymetryon(key: .key65)
        }
    }
    
    var requantitytherm: String {
        switch self {
        case .archjurousance: String.oppolymetryon(key: .key22)
        case .supergenorphyte: String.oppolymetryon(key: .key63)
        case .macroclickenceout: String.oppolymetryon(key: .key62)
        }
    }
}

